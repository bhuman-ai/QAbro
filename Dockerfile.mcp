FROM node:24-slim

WORKDIR /app

ENV NODE_ENV=production

COPY package.json package-lock.json ./
RUN npm install --omit=dev

COPY lib ./lib
COPY scripts ./scripts
COPY api ./api

EXPOSE 8080

CMD ["node", "scripts/qa-mcp-http-server.js"]
